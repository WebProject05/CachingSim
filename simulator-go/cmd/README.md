# Commands

This folder contains runnable programs. Each command assembles configuration, simulation components, and an execution loop; reusable logic belongs under `pkg`.

Run a command from `simulator-go` with `go run ./cmd/<name>` or build it with `go build ./cmd/<name>`.
