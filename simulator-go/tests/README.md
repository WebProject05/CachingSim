# Integration tests

Tests in this folder exercise behavior across simulator packages, including cache state transitions and reward calculations. They complement the focused policy tests in `pkg/baselines`.

Run them with `go test ./tests` or run the complete simulator suite with `go test ./...`.
